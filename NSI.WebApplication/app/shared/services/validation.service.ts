import { Injectable } from '@angular/core';
import { FormGroup, FormControl } from '@angular/forms';

@Injectable()
export class ValidationService {
    static validateEmailFormat(ctrol: FormControl) {
    }

    static validateTimeFormat(ctrol: FormControl) {
    }

    static validateDateFormat(ctrol: FormControl) {
    }
}